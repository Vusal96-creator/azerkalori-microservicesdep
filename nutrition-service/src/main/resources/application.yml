server:
  port: 8084
spring:
  application:
    name: nutrition-service
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5440/azerkalori?currentSchema=nutrition}
    username: ${DB_USER:azer}
    password: ${DB_PASS:azer_strong_pw_2026}
  jpa:
    hibernate:
      ddl-auto: update

grpc:
  server:
    port: 9090
  client:
    nutrition:
      address: static://localhost:9090
      negotiation-type: plaintext

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
